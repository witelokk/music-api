ktor:
    application:
        modules:
            - com.witelokk.ApplicationKt.module
    deployment:
        port: 8080

database:
    url: $DATABASE_URL
    user: $DATABASE_USER
    password: $DATABASE_PASSWORD

redis:
    url: $REDIS_URL

jwt:
    secret: $JWT_SECRET
